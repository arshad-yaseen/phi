// 'as' renames the binding, and nothing else, so both names reach one module
import helper as first
import helper as second

fn drive(seed: i64) i64 {
    return first.twice(seed) + second.twice(seed)
}
