import std/io

fn usage() []u8 {
    return
        \\phi build [options] <root.phi>
        \\
        \\  --run   build, then run it
}

fn main() {
    io.print(usage())
    io.print("\n")

    io.print(
        \\"quotes", \n, // and \\ are the bytes they spell
        \\
    )
}
