type Point = {
    x: i64,
    y: i64
}

fn make(x: i64) Point {
    return Point.{ x: x, y: x + 1 }
}

fn slide(p: Point) i64 {
    return p.x + p.y
}

fn main() {
    var p = make(20)
    p.y = p.x + 22
    _ = slide(p)
}
