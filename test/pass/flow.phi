import std/io

fn answer(a: i64, b: i64) i64 {
    return a * b
}

fn main() {
    if answer(6, 7) == 42 {
        io.print("yes\n")
    } else {
        io.print("no\n")
    }
    loop i in 0..3 {
        io.print("ha")
    }
    io.print("\n")
}
