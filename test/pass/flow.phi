fn answer(a: i64, b: i64) i64 {
    return a * b
}

fn main() {
    if answer(6, 7) != 42 { @trap() }

    var count: i64 = 0
    loop i in 0..3 {
        _ = i
        count += 1
    }
    if count != 3 { @trap() }
}
