//! A file documents itself with `//!`, which attaches to no declaration.
//! A second line continues the same comment.

import std.text.Buffer

/// This one documents the declaration below it.
pub fn named() i64 {
    return 1
}
