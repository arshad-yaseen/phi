fn every_form(n: i64, p: *var i64, holder: Holder) {
    var x: i64 = 0
    x = n
    x += n
    x -= n
    x *= n
    x /= n
    x %= n
    x &= n
    x |= n
    x ^= n
    x <<= n
    x >>= n

    holder.field += n
    p.* += n
    _ = x
}
