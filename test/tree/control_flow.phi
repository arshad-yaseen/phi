fn branches(a: i64) i64 {
    if a > 10 {
        return 1
    } else if a > 5 {
        return 2
    } else if a > 0 {
        return 3
    } else {
        return 4
    }
}

fn cleanup(store: Store) {
    var scratch = store.reset()
    defer scratch.destroy()
    defer report(0)
    return
}
