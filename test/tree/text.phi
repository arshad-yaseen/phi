let greeting = "hello"
let letter = 'a'

fn strings(who: []u8) []u8 {
    let escaped = "a\tb\n"
    let quoted = "she said \"hi\", once"
    let byte = "\x41"
    let crab = "\u{1F980}"
    let empty = ""
    return who
}

fn characters(s: []u8) u32 {
    let newline = '\n'
    let quote = '\''
    let backslash = '\\'
    let wide = '🦀'
    if s[0] == 'a' {
        return 1
    }
    return 0
}

fn multiline(who: []u8) []u8 {
    let usage =
        \\phi build [options] <root.phi>
        \\
        \\  --emit c   write C beside the root
    let raw =
        \\"quotes", \n, and // are the bytes they spell
    let alone = \\one line, opened where it starts
    strings(
        \\an argument
    )
    return who
}

fn positions() {
    strings("literal")
    let table = ["one", "two"]
    let pair = Header.{ magic: "hi", first: 'x' }
}
