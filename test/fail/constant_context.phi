type true
type false
type bool = true | false

fn answer() i64 {
    return 42
}

let branched = if true { 1 } else { 2 }
let left = return 1
let escaped = break
let carried = continue
let called = answer()
let trapped = @trap()
let built = i64.{ x: 1 }

let folded_and = false and true
let folded_not = not false
