fn shapes(a: [8]u32, i: u64) {
    _ = a[1..3]
    _ = a[0..]
    _ = a[i..i + 1]
    _ = a[i * 2..a.len]
    _ = a[0..][1..]
}

// a range may be split across lines, because '..' continues the line above
fn wrapped(a: [8]u32) {
    _ = a[0
        ..2]
}
