/// A contract of the program's own. Where it does not hold, the program stops.
pub fn assert(ok: bool) {
    if not ok {
        @trap()
    }
}

/// A case the program has ruled out. Reaching one stops the program.
pub fn impossible[T]() T {
    @trap()
}
