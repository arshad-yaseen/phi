/// Whether `n` is a power of two. Zero is not one.
pub fn is_power_of_two[T: Unsigned](n: T) bool {
    return n != 0 and (n & (n - 1)) == 0
}

/// The smaller of two numbers.
pub fn min[T: Integer](a: T, b: T) T {
    return if a < b { a } else { b }
}
