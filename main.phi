type Err

fn widen(x: i64 | none) i64 | none | Err {
    return x
}

fn narrow(x: i64 | none | Err) i64 | none {
    if x is Err {
        return none
    }
    return x
}

fn main() {
    _ = narrow(widen(7))
}
